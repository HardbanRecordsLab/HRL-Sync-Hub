# Production (Vercel). The app calls the API via the /api/* rewrite in vercel.json,
# so same-origin works and VITE_API_URL can stay empty. Set it only if you call
# the API on a different host directly.
VITE_API_URL=
