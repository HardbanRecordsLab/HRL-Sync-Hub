# Frontend config. Copy to .env.local for local dev.
# The ONLY variable the app needs: the base URL of the HRL Sync API.
# Leave blank to use same-origin (works when the API is reverse-proxied under /api,
# e.g. the Vercel rewrite in vercel.json).
VITE_API_URL=http://localhost:3001
